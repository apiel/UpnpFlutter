import 'package:flutter/material.dart';

import './App.dart';

// thermostat (complicated)
// mqtt
// itag (ble not working well with flutter)
// timeout callurl

// log refactoring using bloc pattern / + add time / + limit size to 100

void main() {
  runApp(new App());
}
